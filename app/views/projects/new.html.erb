<% content_for :meta_title, "#{t_scoped(:admin).capitalize} - #{t_scoped(:new_project).capitalize}" %>

<div class="container flex flex-col w-full p-5 mx-auto my-5 lg:w-1/2">
  <%= link_to dashboard_path do %>
    <span>&#8592; <%= t_scoped(:projects).capitalize %></span>
  <% end %>
  <h1 class="my-3 text-3xl font-heading"><%= t_scoped(:new_project).capitalize %></h1>
  <%= render 'partials/project_form', project: @project %>
</div>
