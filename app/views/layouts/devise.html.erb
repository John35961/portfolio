<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head_tags' %>
  </head>
  <body class="flex flex-col min-h-screen bg-gray-50">
    <main class="flex-grow">
      <%= yield %>
    </main>
  </body>
</html>
