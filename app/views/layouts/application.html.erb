<!DOCTYPE html>
<html>
  <head>
    <title>Portfolio</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-screen flex flex-col">
  <div class="border-4 fixed w-full top-0 border-blue-500"></div>
  <div class="border-4 fixed h-full right-0 border-blue-500"></div>
  <div class="border-4 fixed w-full bottom-0  border-blue-500"></div>
  <div class="border-4 fixed h-full left-0 border-blue-500"></div>
    <%= render 'shared/navbar' %>
    <main class="container mx-auto flex flex-col grow">
      <%= yield %>
    </main>
  </body>
</html>
